#!/bin/sh

# Format, then re-stage what the formatter touched — otherwise the commit
# records the unformatted content and CI's format:check fails on it.
pnpm nx format:write --uncommitted
git add -u
