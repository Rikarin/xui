# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.angular

pnpm-lock.yaml

.nx/self-healing

# Written by `nx release`, so a release commit would otherwise fail format:check
# on markdown no one hand-edits. Unanchored, to cover per-package changelogs too.
CHANGELOG.md