import gulp from 'gulp';


export function projectTasks() {
  // TODO: copy readme
  // automatically set current year
  // generate schematics for simplier usage
  // update version in dist package.json
  gulp.task('post-build', (done) => done());
}
